name: Run Chromatic when checklist is complete

on:
  check_run:
    types: [completed]

permissions:
  contents: read
  actions: read
  pull-requests: read

jobs:
  chromatic:
    if: >
      contains(github.event.check_run.name, 'checklist') &&
      github.event.check_run.conclusion == 'success'

    runs-on: ubuntu-latest

    steps:
      - name: Extract PR info
        id: pr
        run: |
          PR=$(gh api repos/${{ github.repository }}/commits/${{ github.event.check_run.head_sha }}/pulls --jq '.[0]')
          
          NUMBER=$(echo "$PR" | jq -r '.number')
          SHA="${{ github.event.check_run.head_sha }}"
          
          echo "number=$NUMBER" >> $GITHUB_OUTPUT
          echo "sha=$SHA" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prevent duplicate Chromatic for same commit
        run: |
          RUNS=$(gh run list \
            --workflow chromatic.yml \
            --json headSha,status \
            --jq ".[] | select(.headSha == \"${{ steps.pr.outputs.sha }}\") | select(.status != \"completed\")")

          if [ -n "$RUNS" ]; then
            echo "Chromatic already running for this commit – skipping"
            exit 0
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr.outputs.sha }}
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v1

      - uses: actions/cache@v4
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - run: bun install --ignore-scripts

      - run: bun run build-storybook

      - run: |
          npx chromatic \
            --project-token=c9bbc986d0d6 \
            --exit-zero-on-changes \
            --ci
