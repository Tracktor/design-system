import { TextareaAutosize as Component, TextareaAutosizeProps } from "@mui/material";

const TextareaAutosize = (props: TextareaAutosizeProps) => <Component {...props} />;

export default TextareaAutosize;
