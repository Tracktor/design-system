import Component, { ArticleImageProps } from "@/components/DataDisplay/ArticleImage";

export const ArticleImage = (props: ArticleImageProps) => <Component {...props} />;

export default ArticleImage;
