import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Provider/SnackbarProvider"/>

# `SnackbarProvider`

Provider for the snackbar component.

###  Usage

```typescript jsx
import { Button, SnackbarProvider, useSnackbar } from "@tracktor/design-system";

const { openSnackbar } = useSnackbar();

const App = () => (
  <SnackbarProvider>
    <Button onClick={()=> openSnackbar({message : "Hello", severity: "info"})}>Open Snackbar</Button>
  </SnackbarProvider>
);

export default App;
```

### Props

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Required</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td><code>autoHideDuration</code></td>
    <td>number</td>
    <td>false</td>
    <td>The duration of the snack appears</td>
  </tr>
  <tr>
    <td><code>anchorOrigin</code></td>
    <td>SnackbarOrigin</td>
    <td>false</td>
    <td>Position of the snackbar</td>
  </tr>
  </tbody>
</table>
