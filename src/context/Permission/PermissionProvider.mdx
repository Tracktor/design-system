import { Meta } from '@storybook/blocks';

<Meta title="Provider/PermissionProvider"/>

# `PermissionProvider`

Provider to manage permissions in your application.

### Usage


```typescript jsx
import Home from "./Home";
import { PermissionProvider } from "@tracktor/design-system";

const App = () => (
    <PermissionProvider permissions={["order", "checkout"]}>
      <Home />
    </PermissionProvider>
);

export default App;
```

Then anywhere in your application like `home.tsx` you can use the hook without give permissions again.

```typescript jsx
import { usePermission } from "@tracktor/design-system";

const { hasPermission } = usePermission();
const hasCheckout = hasPermission("checkout");
const hasBill = hasPermission("bill");

console.log(hasCheckout) // true
console.log(hasBill) // false
```

### Props

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Required</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td><code>permissions</code></td>
    <td>string[]</td>
    <td>false</td>
    <td>Array of permissions name</td>
  </tr>
  <tr>
    <td><code>setPermissions</code></td>
    <td>function</td>
    <td>undefined</td>
    <td>Permission setter function</td>
  </tr>
  <tr>
    <td><code>disabledPermissions</code></td>
    <td>string[]</td>
    <td>false</td>
    <td>Array of disabledPermissions name</td>
  </tr>
  <tr>
    <td><code>children</code></td>
    <td>string</td>
    <td>false</td>
    <td>Children</td>
  </tr>
  </tbody>
</table>
