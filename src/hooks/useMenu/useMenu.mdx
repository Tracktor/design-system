import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Hooks/useMenu"/>

# `useMenu`

Utility hook for handle menu

###  Usage

```typescript jsx
import { Button, Menu, MenuItem, useMenu } from "@tracktor/design-system";

const { isMenuOpen, anchorMenu, closeMenu, openMenu } = useMenu();

const App = () => (
  <>
    <Button onClick={openMenu}>Open menu</Button>
    <Menu
      open={isMenuOpen}
      onClose={closeMenu}
      anchorEl={anchorMenu}
    >
      <MenuItem>...</MenuItem>
    </Menu>
  </>
);

export default App;
```

### Available state & function

### Available state & function

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Type</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <tr>
    <td><code>closeMenu</code></td>
    <td>Function</td>
    <td>Handles closing the menu</td>
  </tr>
  <tr>
    <td><code>openMenu</code></td>
    <td>Function</td>
    <td>Handles opening the menu</td>
  </tr>
  <tr>
    <td><code>anchorMenu</code></td>
    <td>HTMLElement</td>
    <td>Anchor element for the menu</td>
  </tr>
  <tr>
    <td><code>isMenuOpen</code></td>
    <td>boolean</td>
    <td>Indicates if the menu is open</td>
  </tr>
  </tbody>
</table>
